unit uPrincipal;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants,
  System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, System.Zip, IdBaseComponent, IdMessage,
  IdIOHandler, IdIOHandlerSocket, IdIOHandlerStack, IdSSL, IdSSLOpenSSL,
  IdComponent, IdTCPConnection, IdTCPClient, IdExplicitTLSClientServerBase,
  IdMessageClient, IdSMTPBase, IdSMTP, IdSSLOpenSSLHeaders, ACBrBase, ACBrMail;

type
  TForm1 = class(TForm)
    ACBrMail1: TACBrMail;
    procedure FormCreate(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
    procedure ZipFolder(const SourceFolder, DestinationZip: string);
    procedure ProcessaNFCe(CNPJ, NOME: String; DATA: TDate);
  end;

var
  Form1: TForm1;

implementation

uses
  IdAttachment;

{$R *.dfm}
{ TForm1 }

procedure TForm1.FormCreate(Sender: TObject);
begin
  IdSSLOpenSSLHeaders.Load;
  ProcessaNFCe('51995523000156', 'GOOPEDIR', date);
end;

procedure TForm1.ProcessaNFCe(CNPJ, NOME: String; DATA: TDate);
var
  Caminho: String;
  CaminhoSalvo: String;

  IdMessage: TIdMessage;
  IdSMTP: TIdSMTP;
  IdSSL: TIdSSLIOHandlerSocketOpenSSL;
  Attachment: TIdAttachment;
begin
  Caminho := 'C:\goopedir\NFCe\Arquivos\Nfce\' + CNPJ + '\' +
    FormatDateTime('yyyymm', DATA);
  ForceDirectories('C:\goopedir\NFCe\Email\');
  CaminhoSalvo := 'C:\goopedir\NFCe\Email\(' + CNPJ + ') ' +
    FormatDateTime('yyyymm', DATA) + '.zip';

  ZipFolder(Caminho, CaminhoSalvo);

  ACBrMail1.From := 'contabilidade@goopedir.com';
  ACBrMail1.FromName := 'GooPedir Contabil';
  ACBrMail1.Host := 'smtp.hostinger.com'; // troque pelo seu servidor smtp
  ACBrMail1.Username := 'contabilidade@goopedir.com';
  ACBrMail1.Password := 'MinhaContabilidade@2023';
  ACBrMail1.Port := '465'; // troque pela porta do seu servidor smtp
  ACBrMail1.SetTLS := false;
  ACBrMail1.SetSSL := true; // Verifique se o seu servidor necessita SSL
  ACBrMail1.AddAddress('allan@goopedir.com', 'Allan Colombo');
  // ACBrMail1.AltBody.Text := Memo2.Lines.Text;
  ACBrMail1.Send;

end;

procedure TForm1.ZipFolder(const SourceFolder, DestinationZip: string);
begin

  TZipFile.ZipDirectoryContents(DestinationZip, SourceFolder);

end;

end.
